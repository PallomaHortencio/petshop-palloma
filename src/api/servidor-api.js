const porta = "2112";

/* const serverApi = `http://localhost:${porta}`; */ // Versão localhost
const serverApi = `http://10.20.45.34:${porta}`; // Versão rede local via api

export default serverApi;
